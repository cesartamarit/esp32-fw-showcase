menu "WiFi Configuration"

    config WIFI_SSID
        string "WiFi SSID"
        default "your_ssid"
        help
            SSID of the access point used for OTA firmware updates.

    config WIFI_PASSWORD
        string "WiFi Password"
        default ""
        help
            Leave empty for open networks.

endmenu
